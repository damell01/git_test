# Composer dependencies
admin/vendor/
vendor/

# Composer lock file (keep composer.json but ignore lock in shared deployments)
# composer.lock

# Environment / local config overrides
.env
*.env
!.env.example
.env.local

# Logs
*.log

# OS files
.DS_Store
Thumbs.db

# IDE files
.idea/
.vscode/
*.sublime-project
*.sublime-workspace

# Upload / cache directories
admin/assets/img/uploads/
admin/cache/
# Uploaded logo/images (keep the directory skeleton but not user uploads)
admin/uploads/*.png
admin/uploads/*.jpg
admin/uploads/*.jpeg
admin/uploads/*.gif
admin/uploads/*.webp
admin/uploads/*.svg
