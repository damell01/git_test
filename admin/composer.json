{
    "name": "trashpanda/rolloffs-admin",
    "description": "Trash Panda Roll-Offs Admin Panel",
    "type": "project",
    "require": {
        "php": "^8.1",
        "phpmailer/phpmailer": "^6.9",
        "stripe/stripe-php": "^13.0"
    },
    "config": {
        "optimize-autoloader": true,
        "sort-packages": true
    },
    "autoload": {
        "psr-4": {
            "TrashPanda\\": "src/"
        }
    }
}
